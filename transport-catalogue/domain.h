#pragma once

/*
 * В этом файле вы можно было бы разместить классы/структуры, которые являются частью предметной области (domain) приложения
 * и не зависят от транспортного справочника. 
 *
 * В данном проекте он остаётся пустым, поскольку было решено сохрнаить структуры остановок и автобусов внутри транспортного каталога 
 *
 */
